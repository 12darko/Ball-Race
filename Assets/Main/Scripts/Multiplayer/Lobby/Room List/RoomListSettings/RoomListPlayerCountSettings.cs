using UnityEngine;

namespace _Main.Scripts.Multiplayer.FusionCore.StartManager.UI.RoomList.RoomListSettings
{
    public class RoomListPlayerCountSettings : MonoBehaviour
    {
        private void Start()
        {
            /*RoomListManager.Instance.SessionMaxPlayerCount = int.Parse(RoomListManager.Instance.RoomListData
                .RoomIsPlayerMaxCountDropDown
                .options[RoomListManager.Instance.RoomListData.RoomIsPlayerMaxCountDropDown.value].text);*/
        }
    }
}