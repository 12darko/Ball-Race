using System;
using UnityEngine;

namespace _Main.Scripts.Multiplayer.FusionCore.StartManager.UI.RoomList.RoomListSettings
{
    public class RoomListPrivacySettings : MonoBehaviour
    {
        private void Start()
        {
            
           /* var privacyValues =   RoomListManager.Instance.RoomListData.RoomPrivacyDropdown
                    .options[  RoomListManager.Instance.RoomListData.RoomPrivacyDropdown.value].text;

                switch (privacyValues)
                {
                    case "ANYONE CAN JOIN":
                        RoomListManager.Instance.SessionIsOpen = true;
                        RoomListManager.Instance.SessionIsVisible = true;
                        break;
                    case "INVITES ONLY":
                        RoomListManager.Instance.SessionIsOpen = true;
                        RoomListManager.Instance.SessionIsVisible = false;
                        break;
                }*/
        }
    }
}